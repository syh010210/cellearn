# SVG 슬라이드 생성 규칙 (수정본 — 기존 규칙 교체용)

기존 규칙의 문제: 텍스트를 "박스 상단부터 아래로 쌓는" 공식만 있고, 박스 높이가
내용보다 여유 있을 때 텍스트를 세로 중앙에 두는 공식이 없었음. 그 결과 텍스트가
항상 위로 쏠리고, 4줄짜리 긴 텍스트는 박스가 작아서 위로 눌리는 문제가 반복됨.
아래 규칙으로 전면 교체할 것.

---

## 1. 캔버스 크기

- width: 고정 960 강제하지 말 것. 내용이 넓으면 1100~1300까지 늘려도 됨.
- height: "보통 400~600" 같은 감으로 잡지 말고, **아래에서 정의하는 방식으로 모든
  박스/텍스트의 y 좌표를 먼저 계산한 뒤, 가장 아래에 있는 요소의 bottom 값 +
  하단 여백 40px**을 최종 height로 정한다. (마지막 박스가 잘리는 문제 방지)
- 전체적으로 여백을 넉넉히 준다. 요소 사이 최소 간격은 24px 이상 (기존보다 여유있게).

## 2. 텍스트 블록 높이 계산 (기존 유지)

N줄 텍스트, 각 줄 폰트 크기 f1...fN 일 때:

```
block_height = f1×0.8 + Σ(f_i × 1.8, i=1..N-1) + fN×0.3
```

이건 그대로 쓴다. 문제는 다음 단계다.

## 3. 텍스트 블록을 박스 안에 배치하는 방법 (핵심 수정 — 반드시 이 순서로)

**Case A: 박스 크기를 내용에 맞춰 새로 만드는 경우**
```
box_height = 14(top padding) + block_height + 14(bottom padding)
first_baseline = rect_y + 14 + f1×0.8
다음 줄부터는 기존 공식대로: baseline_i = baseline_(i-1) + f_(i-1)×1.8
```

**Case B: 박스 높이가 이미 정해져 있는 경우 (다른 박스와 높이를 맞춰야 할 때) — 반드시 세로 중앙 정렬**
```
top_offset = max(14, (box_height - block_height) / 2)
first_baseline = rect_y + top_offset + f1×0.8
다음 줄부터는 동일: baseline_i = baseline_(i-1) + f_(i-1)×1.8
```
→ 이 계산을 생략하고 무조건 14px 고정 padding으로 시작하면 안 됨.
  박스 높이가 내용보다 클 때는 반드시 이 중앙 정렬 공식을 적용한다.

## 4. 가로(수평) 정렬

- 여러 줄로 된 텍스트 블록은 **모든 줄을 `text-anchor="middle"`, `x = box_x + box_width/2`**
  로 통일한다. 일부 줄만 다른 x좌표를 쓰지 않는다.
- 그룹 전체(예: 4줄 수식 목록)를 하나의 박스 안에 넣을 때도 각 줄을 박스의
  가로 중앙에 맞춘다. 왼쪽 정렬이 필요한 표/목록이 아닌 이상 무조건 middle.

## 5. 요소 간 겹침 방지 (새로 추가 — 기존에 없던 규칙)

SVG를 완성하기 전에 다음을 직접 좌표로 확인한다:

- 인접한 두 도형(박스, 화살표, 텍스트)의 bounding box가 겹치지 않는지 확인:
  `boxA.x + boxA.width + gap <= boxB.x` (가로로 나열된 경우)
  `boxA.y + boxA.height + gap <= boxB.y` (세로로 나열된 경우)
  여기서 `gap >= 16px` (화살표가 있는 경우 `gap >= 24px`, 화살표 자체 길이 포함)
- 화살표(`marker-end`)는 시작점/끝점이 박스 테두리에서 최소 4~6px 떨어진 지점에서
  시작/종료해야 한다. 박스 테두리에 붙이거나 박스 안으로 파고들게 그리지 않는다.
- 캡션/설명 텍스트가 박스 바로 아래에 붙는 경우, 박스 하단 테두리와 텍스트 사이
  최소 8~10px 간격을 둔다.
- 박스 안에 있는 텍스트의 마지막 줄 bottom(`baseline + fontSize×0.3`)이
  `rect_y + rect_height`보다 반드시 작은지 수치로 확인한다 (기존 규칙 유지, 계속 지킬 것).

## 6. 폰트 크기 최소값 (가독성 상향 — 기존 수정)

기존 표의 "보조 설명 13-14px"는 나이 있는 사용자에게 너무 작다는 피드백을 반영해
아래로 상향한다:

| 역할 | 기존 | 수정 |
|---|---|---|
| 슬라이드 제목 | 22px bold | 22px bold (유지) |
| 섹션 제목 | 17–18px bold | 18–20px bold |
| 본문 | 15–16px | 16–18px |
| 보조 설명 | 13–14px | **15px 이상** (13-14px 금지) |

하단에 있는 요약/공식 설명 바처럼 실질적으로 사용자가 꼭 읽어야 하는 텍스트는
"보조 설명"이 아니라 "본문" 취급한다. 진짜 부가정보(각주 성격)만 15px을 최소로 쓴다.

## 7. 완성 전 자체 체크리스트 (기존 체크리스트에 추가)

기존 체크리스트(줄 간격, padding, y값 범위 확인)에 아래 3개를 추가:

- [ ] 박스 높이가 내용보다 여유 있는 경우, 텍스트가 3번 규칙의 중앙 정렬 공식으로
      배치되었는가? (위로 쏠려있지 않은가)
- [ ] 인접한 모든 도형/화살표/텍스트 쌍에 대해 5번 규칙의 겹침 검사를 했는가?
- [ ] 캔버스 height가 가장 아래 요소의 bottom + 40px 이상인가? (마지막 요소 잘림 방지)

## 검증 주석 형식 (기존 유지 + 중앙 정렬 케이스 추가)

Case B(중앙 정렬)를 쓴 경우 주석에 반드시 아래처럼 top_offset 계산 과정을 남긴다:

```
<!--
  박스 높이 120 고정 (다른 카드와 맞춤), 내용 2줄 (18px + 15px):
  block_height = 18×0.8 + 18×1.8 + 15×0.3 = 14.4 + 32.4 + 4.5 = 51.3
  top_offset = max(14, (120 - 51.3)/2) = max(14, 34.35) = 34.35
  L1 baseline = rect_y + 34.35 + 18×0.8 = rect_y + 48.75
  L2 baseline = L1 + 18×1.8 = L1 + 32.4
  L2 bottom = L2 + 15×0.3 = L2 + 4.5 < rect_y + 120 ✓
-->
```