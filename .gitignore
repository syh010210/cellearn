# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel
.vercel

# slidegen(개발용 SVG→PNG 렌더 도구) — 앱 빌드와 무관, 저장소/배포 제외
slidegen/node_modules
slidegen/out

# 대용량 원본(런타임 미사용, 로컬 보관용) — 25MB PPT 등 저장소/배포 제외
src/data/lessons/*.pptx

# 기출 검증용 원본·참조 이미지(저작권 · 런타임 미사용, 로컬 보관용) — 저장소/배포 제외
trial_test/
ref_image/

# 환경변수(키) — .env.example 만 커밋한다
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
